version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.14"
  apt_packages:
    - graphviz
  jobs:
    install:
      - pip install --upgrade pip
      - pip install --group docs

sphinx:
  configuration: docs/source/conf.py
